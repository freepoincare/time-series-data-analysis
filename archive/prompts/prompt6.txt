[Request]

In the "Overview" navigation > "Core Research Questions Addressed" section, the first question is currently open by default because of:

if "active_question_id" not in st.session_state:
    st.session_state["active_question_id"] = 1  # Default open first question

Please change this behavior so that all questions are closed by default when the dashboard is first opened.

Users should be able to click a question to expand it, but no question should be expanded initially.

Please make only the necessary change and do not modify the existing question content or styling.

Thanks!

---

[Answer]

  The request from prompt6.txt has been implemented:

  ### Change Summary

  • In app.py (Overview > Core Research Questions section), updated the default initialization of st.session_state["active_question_id"] from 1 to None.
  • When the dashboard is initially loaded, all question cards are now closed by default.
  • Users can click any question card to expand and view its finding, while maintaining the single-open accordion behavior.
  • Verified dry-run execution with 0 errors.

